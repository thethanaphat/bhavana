# ภาวนา (bhavana) — architecture และแผน V0.1

ปรับแผน: 20 กันยายน 2026 · สถานะ: Milestone 5 ผ่านแล้วบน iPhone จริง รวมถึงเกณฑ์เสียงตอนล็อกจอ; กำลังทำ Milestone 6

## เป้าหมายผลิตภัณฑ์

แอปฝึกสติส่วนตัวสำหรับ iPhone: เปิดแล้วเริ่ม **นั่งสมาธิ**, **เดินจงกรม** หรือ **สวดมนต์** ได้ทันที เป็น mobile-first PWA, local-first, ไม่ต้อง login, ไม่มี backend, โฆษณา, analytics, ฟีดสังคม หรือการติดตามผู้ใช้จากภายนอก ข้อมูลการฝึกอยู่ในเครื่องและส่งออก/นำเข้าด้วย JSON ได้

Navigation หลักมีเพียง **ฝึก · บทสวด · บันทึก**; ตั้งค่าอยู่ในเมนูรอง หน้า “ฝึก” แสดงสรุปวันนี้และทางเข้า 3 กิจกรรมแบบแตะง่าย จดค่า timer ล่าสุดของนั่งและเดินแยกกัน

## IC Summary เป็น reference เท่านั้น

เริ่มศึกษาจาก `IC Summary Web & Ebook/README.md` แล้วตรวจ source ที่เกี่ยวข้องกับการอ่านและเสียง ไม่แก้ไฟล์ในโปรเจกต์นั้น

| ส่วนของ IC Summary | สิ่งที่ได้เรียนรู้ |
|---|---|
| `ic-ebook/source/`, `content/`, `assets/`, `scripts/build.py`, `build/` | แยกต้นฉบับ, metadata, หน้าแม่, การตรวจ และ output ที่สร้างใหม่ได้; มีเนื้อหา 22 บท |
| `ic-ebook/scripts/index.py`, `course.py`, `check.py` | สารบัญมาจากข้อมูลกลางและตรวจโครงก่อน build |
| `audio/`, `durations.json`, `scripts/embed_audio.py` | M4A รายบทและความยาวจากไฟล์จริง; ใช้เป็นแนวคิดสำหรับเสียงบทสวดแบบง่าย |
| `web/app.py` | Flask เสิร์ฟ HTML ที่ build แล้วและใช้ `conditional=True` กับเสียงเพื่อรองรับ Range/seek บน Safari iPhone |
| `ic-ebook/assets/shell.html`, `styles.css` | หน้าภาษาไทย, dark mode, responsive layout และการจำสถานะในเครื่อง |

ภาวนา เป็น sibling project แยกขาดจาก IC Summary ไม่ย้าย Flask, SQLite ฝั่งเซิร์ฟเวอร์, Stripe, login, watermark, tracking หรือเนื้อหา/เสียง IC มาใช้ ภาพแอปเดิมทั้ง 4 รูปในโฟลเดอร์นี้เป็นตัวอย่างทาง UX: ตัวเลือกเวลาขนาดใหญ่และสถิติสะสม แต่ไม่คัดลอกตัวตนหรือข้อความของแอปนั้น

## ขอบเขต V0.1

### 1. นั่งสมาธิและเดินจงกรม

ใช้ timer engine เดียวกัน แต่เก็บกิจกรรมเป็น `sitting` และ `walking` แยกกัน มี preset **10, 15, 20, 30, 45, 60, 90 นาที**, กำหนดเอง และแบบไม่กำหนดเวลาจบ รองรับ Start, Pause, Resume, Finish, เสียงระฆังเริ่ม/จบ และระฆังระหว่างทาง **ปิด/ทุก 1/5/10/15 นาที** โดย 1 นาทีเป็นตัวเลือกชั่วคราวสำหรับทดสอบเสียงขณะล็อกจอให้จบในไม่กี่นาที ค่า preset/ระฆังล่าสุดจำแยกตามกิจกรรม

การเริ่มฝึกเป็น 2 ขั้นตามการตรวจหน้าจอของเจ้าของโปรเจกต์: เลือกเวลาแล้วไปหน้าพร้อมเริ่มที่แสดง `00:00` โดยยังไม่สร้าง session หรือเดินเวลา; กด “เริ่มฝึก” บนหน้านั้นจึงบันทึกเวลาเริ่มจริง การกลับเข้าช่วงฝึกที่เริ่มไปแล้วไม่ต้องกดเริ่มซ้ำ

Timer คำนวณ elapsed/remaining จาก timestamp กับช่วง pause ที่บันทึกไว้ ไม่ใช้จำนวนครั้งของ `setInterval` เป็นนาฬิกา `ActiveSession` อยู่ใน IndexedDB เพื่อคืนสถานะหลังล็อกจอ สลับแอป PWA ถูกระงับ หรือเปิดเบราว์เซอร์ใหม่ เมื่อกลับมาให้ตรวจ session ที่ค้างและแสดงทางเลือกทำต่อหรือจบ; บันทึก session ครั้งเดียวตาม ID ตั้งแต่ Milestone 4 เก็บช่วงที่ฝึกจริงแต่ละช่วงไว้ด้วย เพื่อแบ่งเวลาข้ามเที่ยงคืนโดยไม่เอาช่วงพักมานับ

### 2. บทสวดและการบันทึกการสวด

เริ่มด้วย 3 บทหลัก: **อิติปิโส · พาหุง · พระคาถาเงินล้าน** metadata อยู่จุดเดียว ข้อความบทสวดอยู่แยกจาก UI ต้องตรวจแหล่งข้อความก่อนเผยแพร่ หน้าบทอ่านง่ายบน iPhone และปรับขนาดตัวอักษรได้

มี **Prayer Audio แบบง่าย** ต่อบทเมื่อเจ้าของแอปมีไฟล์เสียง: เล่น, หยุด, เริ่มใหม่, แถบความคืบหน้า, เวลาเล่น/ความยาวทั้งหมด ไม่ทำ audiobook หลายบท, playlist, resume position หรือสถิติการฟัง การกดเล่นเสียง **ไม่** บันทึกว่าเป็นการสวด

ผู้ใช้เลือกบันทึกการสวดหรือไม่ก็ได้ `ChantSession` เก็บบทหลัก, ชื่อบท ณ วันที่สวด, จำนวนรอบถ้าระบุ, เวลาเริ่ม/จบ และระยะเวลาถ้าระบุ ไม่แปลงรอบเป็นนาที V0.1 มีสามบทหลักที่มากับแอป และให้ผู้ใช้เพิ่ม/แก้/ลบบทส่วนตัว (`CustomPrayer`) เป็นชื่อบทพร้อมข้อความตามต้องการ ประวัติเดิมยังจำชื่อบท ณ เวลาที่บันทึก แม้เปลี่ยนชื่อหรือลบบทภายหลัง

### 3. เสียงพื้นหลัง

เตรียมหมวด nature (ฝน/ป่า/ลำธาร/ทะเล), ambient (เสียงนุ่ม/ระฆัง) และ chanting สำหรับอนาคต V0.1 มีตัวอย่างเสียงฝนและทำนองบรรเลงแบบวนซ้ำ 48 วินาที สร้างด้วยสคริปต์ในโปรเจกต์ ไม่มีไฟล์จากบุคคลอื่น ระฆังเริ่ม/ระหว่างฝึก/จบเป็นไฟล์สั้นที่แยกจากเสียงพื้นหลัง เสียงพื้นหลังเลือกแยกสำหรับนั่งและเดิน และไม่ pre-cache ไฟล์เสียงพื้นหลัง การพักตัวจับเวลาพักเสียงพื้นหลังด้วย; หลัง reload ต้องแตะเปิดเสียงใหม่ เพราะเบราว์เซอร์ต้องการการกดของผู้ใช้ คุณภาพเสียงต้องให้เจ้าของโปรเจกต์ฟังและยืนยันก่อนเผยแพร่

### 4. บันทึก สถิติ และข้อมูลเดิม

History แยกนั่ง/เดิน/สวด แสดงวันที่, ระยะเวลาจริง, เวลาที่วางแผนไว้, จบตามเวลา/หยุดก่อน, และจำนวนรอบสวดเมื่อมีข้อมูล สถิติมี **วันนี้ · สัปดาห์นี้ · เดือนนี้ · ทั้งหมด**: นั่งและเดินแสดงจำนวนครั้ง/นาที; สวดแสดงจำนวนครั้ง/รอบ และเวลาที่มีการบันทึก ห้ามแปลงรอบเป็นนาที

`LegacyBaseline` เป็นยอด **นั่งสมาธิสะสมจากแอปเก่า** ที่กรอกเองได้ นับรวมในยอดนั่งสะสมทั้งหมดเท่านั้น ไม่สร้าง session ปลอมหรือกระจายไปวัน/สัปดาห์/เดือนก่อนหน้า

สถิติช่วงปัจจุบันคำนวณตาม timezone ของเครื่อง สัปดาห์เริ่มวันจันทร์ นั่ง/เดินนับจำนวน session ที่มีเวลาฝึกจริงในช่วงนั้นและแบ่ง duration ตามช่วงฝึกจริง; สวดนับครั้ง/รอบ/เวลาที่กรอกตามวันที่บันทึก ยอดเดิมแสดงเป็นส่วนเพิ่มใน “ทั้งหมด” โดยไม่เพิ่มจำนวนครั้ง บันทึกเก่าที่ไม่มีข้อมูลช่วงพักใช้เวลาโดยรวมย้อนไปจากเวลาจบเพื่อประมาณการแบ่งวัน

## Technical architecture

- **Frontend:** TypeScript แบบ strict + Vite, โครง UI แยก feature, static hosting ผ่าน HTTPS, ไม่มี server runtime
- **Hosting:** GitHub เป็นทั้งแหล่งจริงของโค้ดและโฮสต์ production ผ่าน GitHub Pages + Actions **ไม่ใช้ Railway** เพราะไม่มี server runtime ให้รัน และ **ไม่ผูกกับโดเมนธุรกิจ** เพราะโปรเจกต์ต้องอยู่ได้แม้เจ้าของหยุดต่ออายุโดเมน Cloudflare Pages/R2 เป็นทางหนีเมื่อชน bandwidth — ดู [docs/DEPLOYMENT.md](docs/DEPLOYMENT.md)
- **เสียงบทสวด:** สร้าง URL ผ่าน `prayerAudioUrl()` ใน `src/content/audio.ts` ที่เดียว รองรับทั้ง path ในโปรเจกต์และ URL เต็ม เพื่อให้ย้ายไฟล์ไป object storage ภายหลังโดยไม่ต้องแก้ UI
- **PWA:** manifest, standalone, ไอคอน, iPhone safe area, service worker สำหรับ app shell, ข้อความบทสวด และไฟล์ระฆังสั้น; cache แบบ versioned
- **ข้อมูล:** IndexedDB เป็นแหล่งจริง; stores สำหรับ `PracticeSession`, `ChantSession`, `ActiveSession`, `Settings`, `LegacyBaseline`, `CustomPrayer`; เตรียม version migration และ export/import JSON ที่ตรวจ schema ก่อนเขียน
- **เนื้อหา:** catalog กลางของ 3 บท + ไฟล์ข้อความแยก UI; เสียงบทสวดผูกตาม ID บทอย่างง่ายเมื่อมีไฟล์
- **เวลา:** เก็บเวลาเป็น UTC; สรุปตาม timezone ในเครื่อง; session ข้ามเที่ยงคืนแบ่งช่วงสำหรับสถิติรายวัน; หลีกเลี่ยงการนับ session ซ้ำเมื่อ recovery
- **เสียง:** `<audio>` เริ่มจากการกดของผู้ใช้ จัดการ `play()` rejection/OS interruption; asset เสียงอ่านและเสียงพื้นหลังแยกจาก bell
- **Privacy:** ไม่ขอชื่อ เบอร์ อีเมล; ไม่ใส่ third-party SDK, remote analytics หรือ remote font; ไม่มีข้อมูล session ส่งออกอัตโนมัติ
- **การล้างข้อมูล:** เมนูจัดการข้อมูลใน Milestone 6 จะให้ส่งออกข้อมูลก่อน และยืนยันก่อนล้างประวัติทั้งหมด เพราะข้อมูลไม่มีบัญชีสำหรับกู้คืน

### รูปแบบข้อมูลหลัก

```text
PracticeSession: id, type(sitting|walking), startedAt, endedAt,
  durationSec, plannedDurationSec|null, status(completed|stopped),
  bellIntervalMin(0|5|10|15), backgroundSoundId|null, runIntervals(optional)
ChantSession: id, prayerId, prayerTitleSnapshot, rounds|null,
  startedAt, endedAt, durationSec|null
ActiveSession: id, type, startedAt, accumulatedPauseMs,
  pausedAt|null, plannedDurationSec|null, settings snapshot,
  completedIntervals(optional), runningSince(optional)
Settings: ค่า preset/ระฆัง/เสียงพื้นหลังล่าสุดแยก sitting/walking, text size, theme
LegacyBaseline: sittingDurationSec, asOfDate, note
CustomPrayer: id, title, text, createdAt, updatedAt
```

## Folder structure ที่เสนอ

```text
ภาวนา/
├── ARCHITECTURE.md
├── README.md
├── index.html
├── package.json
├── tsconfig.json
├── public/
│   ├── manifest.webmanifest
│   ├── icons/
│   └── audio/                  # bells, ambience, prayer audio เมื่อมีไฟล์
├── content/prayers/           # ข้อความบทสวด แยกจาก UI
├── src/
│   ├── app/                   # shell, navigation, route state
│   ├── features/
│   │   ├── practice/          # sitting/walking UI และ timer (Milestone 2)
│   │   ├── prayers/           # catalog, reader, simple audio (Milestone 3)
│   │   ├── chanting/          # บันทึกการสวด (Milestone 3)
│   │   └── records/           # history/statistics (Milestone 4)
│   ├── data/                  # IndexedDB schema, migrations, repository
│   ├── pwa/                   # registration และ service worker
│   ├── styles/                # design tokens, Thai typography, safe area
│   └── main.ts
└── tests/                    # timer, recovery, data migration/statistics เมื่อมี logic
```

`dist/` เป็น build output; ไม่แก้ด้วยมือ

## Milestones และจุดหยุด

| Milestone | งาน | เกณฑ์ตรวจ |
|---|---|---|
| 1 — เสร็จแล้ว | สร้าง TypeScript + Vite, PWA foundation, app shell, navigation, IndexedDB schema, หน้าเริ่มฝึก, UI เลือกเวลา, หน้าบทสวดและบันทึกแบบ placeholder | build/typecheck ผ่าน, เปิดได้บนมือถือ, ไม่มี timer/session จริงหรือเนื้อหาบทสวดที่ยังไม่ตรวจ |
| **2 — เสร็จแล้ว** | timer นั่ง/เดิน, pause/resume, แบบไม่กำหนดเวลา, active-session recovery | ทดสอบ logic เวลาเมื่อออกจากแอปและการพักแล้ว; รอตรวจพฤติกรรมจริงบน iPhone |
| **3 — เสร็จแล้ว** | reader บทสวด 3 บท, เพิ่มบทส่วนตัว, Prayer Audio แบบง่ายเมื่อมีไฟล์, บันทึกการสวด, ปรับเริ่ม timer เป็น 2 ขั้น | ทดสอบ add/edit/delete และ title snapshot ในเบราว์เซอร์; เสียงรอไฟล์จริงและการตรวจบน iPhone |
| **4 — เสร็จแล้ว** | History, สถิติ 4 ช่วง, LegacyBaseline | ทดสอบการแบ่งเวลาข้ามวัน/สัปดาห์และยอดเดิม; ตรวจกรองประวัติในเบราว์เซอร์แล้ว |
| **5 — เสร็จแล้ว** | ระฆังเริ่ม/จบ/ช่วง, เสียงพื้นหลังฝน/ทำนองบรรเลง, keepalive ค้ำ audio session | **ผ่านบน iPhone จริง**: ระฆังดังครบทุกใบขณะล็อกจอ ทั้งเปิดและปิดเสียงพื้นหลัง เสียงพื้นหลังหยุดตรงเวลา ยืนยันถึงรอบ 20 นาที |
| **6 — กำลังทำ** | export/import JSON, ล้างประวัติทั้งหมดพร้อมยืนยัน, offline polish | ส่งออกแล้วนำเข้ากลับได้ครบ, ปฏิเสธไฟล์ที่ schema ไม่ตรง, ล้างข้อมูลต้องยืนยัน; **offline polish ต้องรอ HTTPS เพราะ service worker ไม่ทำงานบน `http://<ip>`** |

จุดหยุดเดิมก่อน Milestone 6 คือการตัดสินใจแนวทางเสียงตอนล็อกจอ ซึ่งปิดแล้ว: **คงเป็น PWA ต่อ ไม่ย้ายไป native iOS และไม่ต้องมี Apple Developer Program**

เหตุผลที่ export/import ขึ้นก่อนในลำดับ Milestone 6: ตอนนี้ข้อมูลผูกกับ origin ที่ใช้เปิดแอป การทดสอบผ่าน `http://<ip ใน LAN>` จึงทำให้ประวัติการฝึกย้ายตามไปโดเมนจริงไม่ได้เลยเมื่อ deploy ยิ่งบันทึกไว้นานยิ่งเสียดาย

### เกณฑ์ความแม่นยำที่ต้องพิสูจน์

ตัวเลขเวลาบนหน้าจอคำนวณจาก timestamp จึงแสดงเวลาที่ผ่านไปได้ถูกต้องเมื่อเปิดแอปกลับมา แต่เสียงระฆังและคำสั่งหยุดเสียงพื้นหลังของ PWA ปัจจุบันใช้ JavaScript; iOS อาจพักการทำงานเมื่ออยู่เบื้องหลัง จึงไม่มีหลักประกันว่าเสียงจะดังหรือหยุดตรงเวลา แม้หน้าจอนาฬิกาจะกลับมาแสดงถูกต้องก็ตาม Web Push ของ iOS ต้องมีเซิร์ฟเวอร์ส่งและไม่ใช่ตัวตั้งเวลาในเครื่องของ PWA

ถ้าเกณฑ์หลักคือระฆังระหว่างทางและเสียงจบ/หยุดตอนล็อกจอตามเวลา ควรคงเว็บเป็น UI และเพิ่ม iPhone app ที่ติดตั้งในเครื่อง ให้ส่วน iOS ดูแลเวลาจบและกำหนด local notifications ไว้ล่วงหน้า พร้อมจัดการ background audio แบบ native โดยไม่เพิ่ม login หรือการส่งข้อมูลส่วนตัว ระฆังจาก notification ยังขึ้นกับการอนุญาตแจ้งเตือน, Silent/Focus และการตั้งค่าเครื่อง จึงต้องทดสอบบน iPhone จริงทั้งเปิดหน้าจอ, ล็อกจอ, สลับแอป, สายเข้า, พัก/ฝึกต่อ และกำหนดเวลาจบ การทำ iPhone app ต้องใช้ Xcode; เครื่องพัฒนาปัจจุบันมีเฉพาะ Command Line Tools

## ผลทดสอบล็อกจอ iPhone (20 กันยายน 2569)

ทดสอบจริงบน iPhone 4 รอบ รอบละ 3 นาที ระฆังทุก 1 นาที พร้อมเสียงฝน:

| รอบ | สภาพหน้าจอ | ระฆังนาที 1 | ระฆังนาที 2 | ระฆังจบ + ฝนหยุด |
|---|---|---|---|---|
| 1 | เปิดค้าง | ดัง | **ไม่ดัง** | ดัง |
| 2 | ล็อกทันทีหลังเริ่ม | ดัง | ดัง | ดัง |
| 3 | เปิดค้าง | ดัง | ดัง | ดัง |
| 4 | ล็อกทันทีหลังเริ่ม | ดัง | ดัง | ดัง |

**ข้อสรุป: iOS ไม่ได้ระงับ timer จนระฆังหายตอนล็อกจอ** ตราบใดที่มีเสียงพื้นหลังเล่นค้างอยู่ หน้าเว็บยังถูกจัดเป็น audible page และ `setInterval` เดินต่อได้ ทั้งระฆังระหว่างฝึกและการหยุดเสียงตอนหมดเวลาทำงานถูกต้อง

ยืนยันซ้ำด้วยรอบ 20 นาที ระฆังทุก 5 นาที เปิดเสียงฝน ล็อกจอตลอด: ระฆังครบทุกใบ ความช้า 0.2-0.3 วินาที

### เสียงคือสิ่งที่ค้ำ timer ไว้ ไม่ใช่ตัว timer เอง

รอบ 10 นาที ระฆังทุก 1 นาที **ปิดเสียงพื้นหลัง** ล็อกจอ ให้ผลตรงข้ามอย่างชัดเจน บันทึกระฆังของรอบนั้นอ่านได้ว่า:

| ระฆัง | สภาพหน้าจอ | ผล |
|---|---|---|
| #1-#5 | ล็อก | **ไม่มีแถวบันทึกเลย** — tick ไม่ได้ทำงานสักครั้ง |
| #6 | ล็อก | ข้าม · ช้า 15.7 วินาที |
| #7 | ล็อก | ข้าม · ช้า 26.6 วินาที |
| #8, #9 | เปิดจอ | ดัง · ช้า 0.8 และ 0.0 วินาที |
| จบ | ล็อกอีกครั้ง | ข้าม · ช้า 15.8 วินาที |

หน้าเว็บที่เงียบสนิทถูกระงับเกือบทั้งหมด แล้วถูกปลุกเป็นครั้งคราวด้วยความหน่วงที่**เพิ่มขึ้นเรื่อย ๆ** (15.7 → 26.6 วินาที) การขยายเพดานความช้าจึงไม่ใช่ทางแก้ เพราะความหน่วงโตไม่มีเพดาน

ทางแก้คือ `ambience/keepalive.m4a` — โทน 40 Hz ที่ระดับ -70 dB เล่นวนตลอดช่วงที่ฝึกอยู่ ไม่ว่าผู้ใช้จะเลือกเสียงพื้นหลังหรือไม่ ลำโพงโทรศัพท์สร้างความถี่ 40 Hz ไม่ได้อยู่แล้ว และระดับเสียงต่ำกว่าที่หูจับได้มาก แต่เพียงพอให้ iOS ถือว่าหน้านี้ยังเล่นเสียงอยู่ ไม่ใช้ความเงียบดิจิทัลล้วนเพราะสตรีมที่เป็นศูนย์ทั้งหมดอาจถูกตัดทิ้งแทนที่จะนับเป็นการเล่น

`startKeepalive()` ต้องถูกเรียกในจังหวะที่ผู้ใช้แตะปุ่มเริ่มฝึกหรือฝึกต่อ เพราะข้อจำกัดเรื่องการปลดล็อกราย element ข้างล่าง และจะหยุดเมื่อกดพัก จบ หรือยกเลิก

**ผลข้างเคียงที่ต้องยอมรับ:** การฝึกทุกครั้งจะเข้าครองช่องเสียงของเครื่อง ถ้าผู้ใช้กำลังเปิดเพลงหรือพอดแคสต์อยู่ เสียงนั้นจะถูกหยุด ซึ่งเดิมเกิดอยู่แล้วเมื่อเปิดเสียงพื้นหลัง แต่ตอนนี้เกิดกับทุกการฝึก

เท่ากับว่า **ยังไม่มีเหตุผลต้องย้ายไป native iOS** สำหรับ V0.1 และไม่ต้องจ่าย Apple Developer Program ตอนนี้ สิ่งที่ยังไม่ได้ทดสอบคือช่วงยาวกว่า 3 นาที, กรณีไม่เปิดเสียงพื้นหลังเลย (หน้าเว็บจะเงียบสนิทและอาจถูกระงับ), สายเข้าแทรก และ Low Power Mode

รอบที่ 1 ที่ระฆังนาทีที่ 2 หายไปยังสรุปไม่ได้ว่าเป็นข้อผิดพลาดของเครื่องหรือของผู้ทดสอบ จึงแก้สองอย่าง: ขยายเพดานความช้าที่ยอมให้ระฆังยังดังจาก 2.5 เป็น 15 วินาที (`bellLatenessLimitMs`) และเพิ่มบันทึกระฆังราย session เพื่อให้ครั้งต่อไปพิสูจน์ได้แทนการเดา

## play() ที่สำเร็จ ไม่ได้แปลว่าได้ยิน

รอบทดสอบ 10 นาทีที่ปิดเสียงพื้นหลังและเปิด keepalive ให้ผลที่ขัดกันเอง: บันทึกระฆังรายงานว่าดังครบ 11 ใบ ความช้าคงที่ 1.5 วินาทีทุกใบ แต่ผู้ทดสอบไม่ได้ยินระฆังนาทีที่ 2 และ 9

แปลว่าเกณฑ์ที่ใช้วัดผิดตั้งแต่ต้น โค้ดเดิมถือว่า `play()` ที่ resolve สำเร็จคือ "ดัง" ซึ่งไม่จริงบน iOS ระบบอาจรับคำสั่งเล่นไว้โดยไม่ส่งเสียงออกลำโพง และไม่มี error ใด ๆ

ลองวัดด้วย `webkitAudioDecodedByteCount` ของ WebKit ก่อน แต่ **Safari บน iPhone จริงไม่มีค่านี้ให้** ทุกบรรทัดจึงขึ้นว่ายังไม่ได้วัด ใช้ไม่ได้

ตอนนี้วัดจาก `currentTime` แทน: อ่านหลังสั่งเล่น 1.2 วินาที ถ้าหัวอ่านเดินหน้าไปจริงแปลว่ามีเสียงออก ถ้าค้างที่ 0 แปลว่า iOS รับคำสั่งไว้เฉย ๆ บันทึกจะขึ้น `⚠️ ไม่มีเสียงออก` พร้อม `readyState` และรหัส error ของ element

## ระฆังแยกกันด้วยจำนวนครั้ง ไม่ใช่ระดับเสียง

เดิมระฆังเริ่มกับระฆังระหว่างฝึกตีครั้งเดียวเท่ากัน ต่างกันแค่โน้ต C5 กับ E5 ซึ่งฟังผ่านลำโพงโทรศัพท์แล้วแยกไม่ออก เพราะห่างกัน 60 วินาทีและไม่มีอะไรให้เทียบ

เปลี่ยนมาแยกด้วยจำนวนครั้งที่ตี ซึ่งได้ยินชัดแม้เสียงเบาหรืออยู่ไกล:

| ไฟล์ | จำนวนครั้ง | โน้ต | ความยาว |
|---|---|---|---|
| `start.m4a` | 2 (ที่ 0.00 และ 0.62 วินาที) | C5 ทั้งสองครั้ง | 3.0 วินาที |
| `interval.m4a` | 1 | E5 | 2.2 วินาที |
| `end.m4a` | 4 (ทุก 0.70 วินาที) | G5 → E5 → C5 → C5 ไล่ลง | 4.8 วินาที |

ระฆังจบไล่เสียงลงมาจบที่โน้ตหลักสองครั้ง เพื่อให้รู้สึกเป็นการคลี่คลายไม่ใช่การเตือน

ความยาว 4.8 วินาทีของระฆังจบชนกับจังหวะที่เดิมดับเสียงค้ำทันทีหลังสั่งเล่น ซึ่งเสี่ยงให้ audio session ถูกปิดแล้วตัดระฆังขาดกลางคัน `stopKeepaliveAfterBell()` จึงรอเหตุการณ์ `ended` ของระฆังก่อน พร้อมตัวกันเหนียว 7 วินาทีเผื่อระฆังเล่นไม่ออกตั้งแต่แรก

## ระฆังใบที่ #2 กับเส้นทาง seek

ทดสอบบน iPhone หลายรอบแล้วพบว่าใบที่เงียบกระจุกอยู่ที่ `#2` อย่างมีนัย (รอบ 10 นาทีและรอบ 4 นาทีเงียบใบเดียวกัน อีกรอบ 4 นาทีดังครบ) เทียบกับโค้ดแล้ว `#2` คือระฆังใบแรกที่วิ่งผ่านเส้นทาง `currentTime = 0` ย้อนไฟล์เดิม ขณะที่ `เริ่ม`, `#1` และ `จบ` มากับการเปลี่ยน `src` ซึ่งไม่เคยพลาดเลยสักรอบ

จึงตัดเส้นทาง seek ทิ้ง ให้ทุกใบตั้ง `src` แล้ว `load()` ใหม่ทุกครั้ง แลกการโหลดซ้ำกับการเริ่มจากต้นไฟล์ที่แน่นอน ไฟล์อยู่ใน HTTP cache อยู่แล้วจากการ warm ตอนสร้าง `PracticeAudio`

**ทดสอบบน iPhone หลังแก้แล้ว: ระฆังดังครบทุกนาที** ปิดปมนี้ได้

เพิ่มการเฝ้าดู `keepalive` ด้วย ถ้ามันถูกหยุดโดยที่แอปไม่ได้สั่ง เช่นสายเข้าหรือแอปอื่นแย่งช่องเสียง จะบันทึกเป็น `เสียงค้ำ — ถูกขัดจังหวะ` พร้อมเวลา เพราะถ้าตัวค้ำดับ timer จะถูกระงับทันทีโดยไม่มีอะไรบอก

## บันทึกระฆังสำหรับไล่ปัญหา

`src/features/practice/bellLog.ts` เก็บทุกเหตุการณ์ระฆังลง `localStorage` พร้อมความช้าเป็นวินาทีและผลลัพธ์ (`ดัง` / `ข้าม เพราะมาช้าเกินกำหนด` / `เล่นไม่ได้` พร้อมชื่อ error) แล้วแสดงบนหน้าสรุปหลังจบการฝึก

ใช้ `localStorage` แทน IndexedDB เพื่อเลี่ยง schema migration เพราะเป็นข้อมูลไว้ไล่ปัญหา ไม่ใช่ข้อมูลการฝึก หายได้โดยไม่กระทบอะไร เมื่อยืนยันความเสถียรแล้วถอดออกได้ทั้งโมดูล

## iOS ปลดล็อกเสียงเป็นราย element

ยืนยันบน iPhone จริงแล้วว่า iOS ให้สิทธิ์เล่นเสียงกับ `<audio>` **ทีละ element** ไม่ใช่ทั้งหน้า element ใดไม่เคยถูก `play()` ในจังหวะที่ผู้ใช้แตะจริง จะถูกปฏิเสธไปตลอดอายุของหน้านั้น

โครงเดิมแยกระฆังเป็น 3 element (`start`, `interval`, `end`) จึงมีแต่ระฆังเริ่มที่ดัง เพราะมันเล่นในจังหวะกดปุ่มเริ่มฝึก ส่วนระฆังระหว่างฝึกและระฆังจบไม่เคยได้เล่นตอนแตะเลย อาการคือ **เงียบสนิทแม้เปิดหน้าจอค้างไว้** และไม่มี error ให้เห็นเพราะโค้ดเดิม `catch` ทิ้ง

แก้โดยใช้ `<audio>` ตัวเดียวแล้วสลับ `src` ระฆังใบแรกที่ดังในจังหวะแตะจึงปลดล็อกให้ใบที่เหลือทั้งหมด และเพราะแต่ละไฟล์จบด้วยตัวมันเอง จึงไม่ต้องพึ่ง JavaScript มาสั่งหยุด ซึ่งสำคัญกับเป้าหมายเรื่องล็อกจอ ไฟล์ระฆังทั้งสามถูก warm เข้า HTTP cache ตอนสร้าง `PracticeAudio` เพราะ element เดียว preload ได้ทีละไฟล์

`playBell` คืนสาเหตุที่เล่นไม่ได้ออกมาแทนการกลืนเงียบ และ `ringBell` ใน `App` แสดงบนหน้าจับเวลา

## ส่งออกและนำเข้าข้อมูล

`src/data/backup.ts` เก็บ schema และ **ตัวตรวจไฟล์แบบ pure** แยกจาก IndexedDB เพื่อให้ทดสอบได้โดยไม่ต้องมีเบราว์เซอร์ ส่วน `src/data/backupRepository.ts` ทำงานกับฐานข้อมูล

รูปแบบไฟล์: `{ format: 'bhavana-backup', version: 1, exportedAt, settings, practiceSessions, chantSessions, customPrayers, legacyBaseline }` ชื่อไฟล์เป็น `bhavana-YYYY-MM-DD.json`

การตัดสินใจที่สำคัญสามข้อ:

- **มีแถวเสียแถวเดียว ปฏิเสธทั้งไฟล์** ไม่นำเข้าบางส่วนเงียบ ๆ เพราะผู้ใช้จะไม่มีทางรู้ว่าอะไรหายไป ข้อความจะบอกลำดับแถวและชื่อฟิลด์ที่ผิด
- **การตั้งค่าที่ผิดรูปไม่ทำให้ไฟล์ใช้ไม่ได้** ถอยไปใช้ค่าเริ่มต้นแทน เพราะการตั้งค่าสร้างใหม่ได้ง่าย ต่างจากประวัติที่หายแล้วหายเลย
- **นำเข้าคือการแทนที่ทั้งหมด ไม่ใช่การรวม** เพราะการรวมทำให้ผู้ใช้เดาผลลัพธ์ไม่ออก หน้าจอจึงแสดงจำนวนของเดิมเทียบกับในไฟล์ก่อนให้ยืนยัน

`clearHistory()` ล้างเฉพาะ `practiceSessions`, `chantSessions`, `legacyBaseline` และ `activeSession` ส่วนบทสวดส่วนตัวกับการตั้งค่ายังอยู่ เพราะเป็นเนื้อหาที่ผู้ใช้สร้าง ไม่ใช่สถิติ

ดาวน์โหลดใช้ blob URL กับ `<a download>` เพราะไม่มีเซิร์ฟเวอร์ให้ยิงไฟล์กลับมา บน iOS Safari จะขึ้นแผ่นบันทึกลง Files ให้เอง

## secure context กับการทดสอบผ่าน LAN

ระหว่างทดสอบจาก iPhone ผ่าน `http://<ip>:4173` เบราว์เซอร์ถือว่าไม่ใช่ secure context จึงตัด `crypto.randomUUID`, service worker และ `navigator.wakeLock` ออกทั้งหมด อาการที่เจอจริงคือกดเริ่มฝึกแล้วนาฬิกาค้างที่ `00:00` เพราะการสร้าง `sessionId` throw เงียบ ๆ ตั้งแต่บรรทัดแรก

ทางแก้คือ `newId()` ใน `src/data/id.ts` ที่ถอยไปใช้ `crypto.getRandomValues` (ซึ่งใช้ได้นอก secure context) และถอยไปใช้ `Math.random` เป็นชั้นสุดท้าย id เหล่านี้เป็นเลขอ้างอิงของ record ในเครื่อง ไม่ใช่โทเคนความปลอดภัย จึงยอมรับได้ ส่วน service worker และ wake lock ยังต้องทดสอบบน HTTPS เท่านั้น

## ความเสี่ยงบน iOS PWA

| เรื่อง | ผลและแนวทาง |
|---|---|
| background/lock-screen timer | iOS อาจ throttle หรือ suspend JavaScript; ใช้ timestamp เพื่อให้เวลาที่เห็นตอนกลับมาถูกต้อง แต่ระฆังระหว่างล็อกจอ/เสียงจบเวลาเป็น **best effort** จนกว่าจะทดสอบจริง หากเสียงพื้นหลังยังเล่นระหว่างล็อกจอ อาจหยุดช้ากว่าเวลาที่ตั้งไว้จนกว่าจะเปิดแอปกลับมา |
| การเล่นเสียง | เริ่มจาก user gesture; Safari และ standalone PWA อาจทำงานต่างกัน; ต้องลอง play/pause, lock screen และเสียงโทรศัพท์แทรกบน iPhone จริง |
| การผสมเสียง | เสียงพื้นหลังกับ bell อาจแย่ง audio focus; แยก player และทดสอบก่อนรับปากการเล่นพร้อมกันขณะล็อกจอ |
| พื้นที่จัดเก็บ | IndexedDB/Cache อาจเต็มหรือถูกลบ; จัดการ error และให้ export/import; ไฟล์เสียงยาวเป็น opt-in/online ก่อน |
| เนื้อหาบทสวด | ต้องตรวจข้อความและสิทธิ์ไฟล์เสียงก่อนเผยแพร่; Milestone 1 จึงใช้ placeholder |

แหล่งอ้างอิงแพลตฟอร์ม: [WebKit: Home Screen web apps](https://webkit.org/blog/13878/web-push-for-web-apps-on-ios-and-ipados/), [WebKit: background throttling/suspension](https://webkit.org/blog/8970/how-web-content-can-affect-power-usage/), [WebKit: storage policy](https://webkit.org/blog/14403/updates-to-storage-policy/), [WebKit: audio in standalone PWA](https://bugs.webkit.org/show_bug.cgi?id=198277/).
