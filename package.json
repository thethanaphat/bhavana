{
  "name": "bhavana",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "check": "tsc --noEmit",
    "test": "tsc --ignoreConfig src/features/practice/timer.ts src/features/practice/bells.ts src/features/records/stats.ts src/data/id.ts src/data/backup.ts --target ES2022 --module ESNext --moduleResolution bundler --rootDir src --outDir .test-build --skipLibCheck && node --test tests/*.test.mjs",
    "build": "npm run check && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.3.0"
  }
}
