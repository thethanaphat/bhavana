node_modules/
dist/
.test-build/
.DS_Store
*.local

# ภาพหน้าจอจากแอปอื่นที่ใช้เป็นตัวอย่าง UX เท่านั้น
# ไม่ commit เพราะ repo นี้ตั้งใจให้เปิดสาธารณะได้ และภาพไม่ใช่งานของโปรเจกต์นี้
S__*.jpg

# iCloud สร้างไฟล์ซ้ำชื่อลงท้าย " 2" เมื่อโฟลเดอร์บน Desktop ถูกเปลี่ยนชื่อระหว่างกำลังซิงก์
# กันไม่ให้หลุดเข้า repo อีก
* 2.*
* 2
